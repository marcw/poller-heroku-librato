# Poller/Heroku/Librato

A proof of concept of a dead simple monitoring application hosted on heroku.

* Edit and run `./setup.sh`
* As this is a "web" process, Heroku will send SIGTERM when it is considered as "idling"
